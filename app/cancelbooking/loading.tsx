// Route-level fallback. Shown on a cold load or hard refresh; the in-page
// skeletons in components/common/Skeleton.tsx cover the data fetch that
// follows. See that file for why these were consolidated.
export { PageSkeleton as default } from "@/components/common/Skeleton";
