// ── Inventory Model (Mongoose)
// TODO: Implement when backend/MongoDB is ready.
//
// import mongoose, { Schema, model, models } from "mongoose";
//
// const InventorySchema = new Schema({
//   category: { type: String, required: true, enum: ["Pool & Chemicals","Furniture & Misc","Cleaning Tools"] },
//   name: { type: String, required: true },
//   qty: { type: Number, required: true, default: 0 },
//   unit: { type: String, required: true },
//   minQty: { type: Number, required: true, default: 0 },
//   notes: { type: String, default: "" },
// }, { timestamps: true });
//
// export const Inventory = models.Inventory || model("Inventory", InventorySchema);
